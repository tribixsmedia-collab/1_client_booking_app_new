<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Android 12 replaced the old launch window with a system splash screen
      that every app gets whether it wants one or not. It cannot be switched
      off, so the next best thing is to make it invisible: white, matching
      the Flutter splash behind it, with an icon that draws nothing.

      Without this the OS falls back to the launcher icon, which is what put
      a second, cropped logo in front of the app on every cold start.
    -->
    <style name="LaunchTheme" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:isLightTheme">true</item>
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:windowSplashScreenBackground">@color/launch_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/launch_splash_icon</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/launch_background</item>
        <item name="android:isLightTheme">true</item>
        <item name="android:forceDarkAllowed">false</item>
    </style>
</resources>
